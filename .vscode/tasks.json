{
    "version": "2.0.0",
    "tasks": [
            {
                    //"type": "cppbuild",
                    "type": "shell",
                    "label": "g++ build active file",
                    "command": "mkdir -p build; cd build && cmake -DCMAKE_BUILD_TYPE=Debug .. && make"
            }
    ]
}